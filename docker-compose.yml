services:
  app:
    container_name: "cookie_lms"
    build: .
    ports:
      - '8080:8080'
    environment:
      WAIT_HOSTS: database:27017
      GOCACHE: /.go-build/

  database:
    container_name: "database_m"
    image: mongo
    attach:
      false
    ports:
      - '27017:27017'
